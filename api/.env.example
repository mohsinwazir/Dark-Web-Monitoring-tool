# ============================================================
# ENVIRONMENT VARIABLES - SECURITY CONFIGURATION
# ============================================================
#
# CRITICAL: Never commit this file with real secrets to Git!
# Add .env to .gitignore immediately
#
# For production: Use proper secrets management (AWS Secrets Manager,
# Azure Key Vault, HashiCorp Vault, etc.)
# ============================================================

# API Security
API_KEY=CHANGE_THIS_TO_STRONG_RANDOM_KEY_IN_PRODUCTION

# Allowed CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# MongoDB Connection
MONGO_URI=mongodb+srv://scrapy:password123123@cluster0.5pobrvy.mongodb.net/
MONGO_DATABASE=darkweb_intelligence

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Email Notifications (Optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=alerts@company.com
SMTP_TO=team@company.com,ceo@company.com

# Slack Notifications (Optional)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Ollama/LLM Configuration
OLLAMA_HOST=http://localhost:11434
LLM_MODEL=llama3

# Rate Limiting
MAX_REQUESTS_PER_MINUTE=60
CRAWL_LIMIT_PER_MINUTE=5
REPORT_LIMIT_PER_HOUR=10

# Session & JWT (if implementing)
SECRET_KEY=GENERATE_STRONG_SECRET_KEY_HERE
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Production Flags
ENVIRONMENT=development  # development | production
DEBUG=True
ENABLE_DOCS=True  # Disable Swagger UI in production for security
